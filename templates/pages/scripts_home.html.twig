{% extends 'base.html.twig' %}

{% block title %} Movie Scripts{% endblock %}

{% block body %}
{% if ScriptTitles %}
    <table id="scripts" class="table table-striped">
        <thead>
            <tr>
                <th> Movie Script</th>
            </tr>
        </thead>
        <tbody>
            {% for ScriptTitle in ScriptTitles %}
                <tr>
                    <td> {{ ScriptTitle.id }}</td>
                    <td>
                        <a href="/script/{{ ScriptTitle.id }}" class="btn btn-dark">Info</a>
                        <a href="/scripts/edit/{{ ScriptTitle.id }}" class="btn btn-light">Edit</a>

                    </td>
                </tr>
                {% endfor %}
            </tbody>
    </table>
    {% else %}
        <p> No data to display. </p>
{% endif %}
{% endblock %}

{% block javascripts %}
    <script src="/js/main.js"></script>
{% endblock %}